version: "2"
sql:
  - engine: "postgresql"
    schema: "migrations"
    queries: "app/db/query.sql"
    gen:
      go:
        package: "db"
        out: "app/internal/db"
        sql_package: "pgx/v5"